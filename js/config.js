// 調整パラメータ。単位はピクセル/秒。手触りはここで詰める。
// 「モトクロスマニアックス」風：アクセル＋ターボに加え、空中で前後リーン(↑↓)
// してフリップ／着地姿勢を制御。CPUライバルとの周回レース。
export const config = {
  DT: 1 / 120,       // 物理の固定タイムステップ(秒)
  GRAVITY: 1500,     // 重力加速度(px/s^2)
  PX_PER_M: 12,      // 1メートルあたりのピクセル

  // --- 地上 ---
  ACCEL: 680,        // アクセル(ホールド)の推進力。巡航 = ACCEL/DRAG
  TURBO: 560,        // ターボの上乗せ加速
  DRAG: 0.95,        // 速度比例の抵抗
  SLOPE_GAIN: 0.32,  // 坂の加減速の効き
  V_MAX: 1400,       // 速度上限(暴走防止)
  V_START: 160,      // スタート/リスポーン初速

  // --- ターボ(ゲージ制。パネルで回復。配分が勝負どころ) ---
  TURBO_MAX: 100,
  TURBO_BURN: 42,    // ホールド中の毎秒消費
  TURBO_PICKUP: 26,  // パネル1枚の回復量
  TURBO_START: 45,

  // --- 空中リーン(前後回転) ---
  LEAN_ACCEL: 8,     // ↑↓ の角加速度(rad/s^2)
  LEAN_AV_MAX: 5,    // 角速度上限(rad/s)
  AIR_PITCH: 0.8,    // 無操作だと前のめりになる自然トルク(rad/s^2。控えめ)
  LAND_TOL: 0.6,     // 着地許容角(地形傾きとの差。超えるとクラッシュ)
  WHEELIE: 0.45,     // 地上ウィリーの見た目の傾き(rad)

  // --- ループ / スプリング / トンネル ---
  LOOP_SAFETY: 1.12, // ループ必要速度の安全係数
  SPRING_VY: 900,    // スプリングの打ち上げ初速(上向き)
  TUNNEL_GAP: 140,   // 地面〜天井の高さ(これ以上飛ぶと頭をぶつけてクラッシュ)

  // --- レース ---
  LAPS: 3,           // 周回数
  TIME_LIMIT: 150,   // フェイルセーフの制限時間(秒)
  RESPAWN_DELAY: 1.3,// クラッシュ後の復帰待ち(秒。レースは進み続ける)
  RESPAWN_RUNUP: 380,// 復帰地点からギャップまで確保する助走距離(px)
  RESPAWN_TURBO: 30, // 復帰時に最低限確保するターボ量
  COUNTDOWN: 2.6,    // スタート前のカウントダウン(秒)
  RIVAL_ACCEL: 765,  // ライバルの実効推進(cruise = RIVAL_ACCEL/DRAG)

  SEED: 2024,        // コース生成シード(固定＝毎回同じサーキット)
};
