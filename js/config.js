// 調整パラメータ。単位はピクセル/秒。手触りはここを触って詰める。
// モトクロスマニアックス風タイムアタック：常時前進するエンジン＋ターボで、
// 制限時間内に周回コースのゴールを目指す。
export const config = {
  // 物理（速度 v も座標もピクセル基準で統一）
  GRAVITY: 1500,     // 重力加速度(px/s^2)。空中の落下と坂加速の基準
  SLOPE_GAIN: 0.30,  // 坂の加減速の効き
  ENGINE: 380,       // エンジンの基本推進(常時前進。これとDRAGで巡航速度が決まる)
  TURBO_THRUST: 640, // ターボの上乗せ加速
  DRAG: 1.25,        // 速度比例の抵抗。巡航 v*=ENGINE/DRAG, ターボ時=(ENGINE+TURBO)/DRAG
  V_START: 200,      // スタート/リスポーン時の初速(px/s)
  DT: 1 / 120,       // 物理の固定タイムステップ(秒)

  // 表示・スコア
  PX_PER_M: 12,      // 1メートルあたりのピクセル

  // レース構成
  LAPS: 2,           // 周回数
  TIME_LIMIT: 50,    // 制限時間(秒)。これを超えるとタイムアップ
  PAR_RATIO: 0.70,   // ライバルが制限時間×この割合でゴールするペースで走る
  RESPAWN_DELAY: 1.2,// クラッシュ後リスポーンまでの待ち(秒。時計は止まらない)

  // ターボ(旧ニトロ)
  TURBO_MAX: 100,
  TURBO_BURN: 30,    // ホールド中の毎秒消費
  TURBO_PICKUP: 32,  // パネル1枚の回復量
  TURBO_START: 55,   // 開始時のターボ量

  // ループ
  LOOP_SAFETY: 1.15, // ループ必要速度の安全係数(難易度はこの1つで調整)

  // スプリング(ジャンプ台)
  SPRING_VY: 880,    // 打ち上げ初速(上向き)

  // トンネル
  TUNNEL_GAP: 130,   // 地面から天井までの高さ(これ以上飛ぶと頭をぶつけてクラッシュ)

  // コースは固定シードで生成（タイムアタックの公平性のため毎回同じ地形）
  SEED: 1337,
};
