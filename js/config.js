// 調整パラメータ。単位はピクセル/秒。手触りはここを触って詰める。
export const config = {
  // 物理（速度 v も座標もピクセル基準で統一）
  GRAVITY: 1500,     // 重力加速度(px/s^2)。空中の落下と坂加速の基準
  SLOPE_GAIN: 0.22,  // 坂の加減速の効き(控えめ＝ニトロ主役)
  NITRO_THRUST: 520, // ニトロ推進加速度(主役なので坂より強い)
  DRAG: 0.5,         // 速度比例の抵抗(無噴射だと数秒で失速する効き)
  V_MIN: 60,         // これを下回ると失速クラッシュ(px/s)
  V_START: 300,      // リスタート時の初速(px/s)
  DT: 1 / 120,       // 物理の固定タイムステップ(秒)
  // 表示・スコア
  PX_PER_M: 12,      // 1メートルあたりのピクセル
  // ニトロ
  NITRO_MAX: 100,
  NITRO_BURN: 35,    // タップ中の毎秒消費
  NITRO_PICKUP: 40,  // カプセル1個の回復量
  NITRO_START: 60,   // 開始時のニトロ量(温存の駆け引きのため満タンにしない)
  // ループ
  LOOP_SAFETY: 1.15, // ループ必要速度の安全係数(難易度はこの1つで調整)
};
