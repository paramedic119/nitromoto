# BLUEBIRD — 開発メモ

無限に続く山を滑り降りるスキーゲーム。**ライブラリ・ビルド・外部アセットはゼロ**。
素の JavaScript（ES Modules）+ WebGL2 + Web Audio だけで動く。
仕様の全体像は [README.md](README.md) と [docs/design/bluebird-design.md](docs/design/bluebird-design.md)。

## 動かす / テスト

```bash
node server/server.js        # http://localhost:8080/ （静的配信 + WebSocket リレー）
node --test                  # 23 件。地形と物理の不変条件を検証する
```

`file://` では動かない（ES Modules のため）。必ずサーバ経由で開くこと。
依存パッケージは**入れない**。`package.json` に dependencies を足す変更は方針違反。

## 壊してはいけない不変条件

テストがあるものはテストで守っている。**変更前に `node --test` を通すこと。**

1. **地形は決定論的な純関数**（`js/world/terrain.js`）
   オンラインは「全員が同じシードから同じ山を得る」ことだけで成立している。
   `Math.random()` や時刻に依存する値を地形へ持ち込むと、全員の山がズレる。

2. **山は必ず下っている**
   マクロノイズは落下線(z)方向に強く引き伸ばしてあり、z 方向の勾配が基本斜度を
   超えないようにしてある。ここを緩めると「窪地にハマって二度と動けない」場所ができる。

3. **圧雪路は横方向にならしてある**（`heightP` の「圧雪路のならし」）
   素の斜面をそのまま使うと、横断勾配に押し流されて無操作でコースから出る。
   `hCenter` へ 0.92 寄せ、さらに `cxSlope` ぶん進行方向へ傾けて、
   コースの落下線とコースの向きを揃えている。**ここが「道がある」感触の全部。**

4. **FLOW が最高速そのものを上げる**（`js/play/rider.js` の `CFG`）
   「丁寧に滑る → 速くなる → 丁寧に滑るのが難しくなる」という自己調整ループ。
   難易度を押し付ける仕組みは意図的に入れていない。ゆっくり滑るのも正解。

## つまずきやすい所（実際に踏んだもの）

- **法線から落下線を出すとき**、最急降下方向は `(n.x, n.z)` に**比例**する（符号を逆にしない）。
- **板の前方向を接地面へ「投影」してはいけない**。水平の向きまで回ってしまい、
  横断勾配のある斜面で操作していないのに進路が曲がる。
  正しくは「水平成分が yaw と一致する接ベクトル」を作る（`_stepGround` 参照）。
- **ロールの符号**：正のロールで右へ傾く＝右ターンで内側へ倒れ込む。
  カメラのバンクと向きが揃っているか、変えたら必ず絵で確かめること。
- **板は剛体の梁**（`_ground`）。有効エッジ長より短い波長の凹凸は跨いで均す。
  これを外すと高速域が細かいノイズを全部拾って暴れる。
- **雪の抵抗は 2 成分**（`mu` と `plow`）。速度非依存の一定値にすると、
  緩斜面のパウダーで完全停止して復帰できなくなる。
- **シェーダの高周波は画面密度でフェードする**（`px = fwidth(...)`）。
  距離だけでフェードするとモアレが残る。きらめきのセルサイズも `px` に追従させている。
- `terrain.js` の `profileAt()` は**共有オブジェクトを 1 個だけ使い回す**。
  `height()` を呼ぶと中身が別の z のものに書き換わるので、
  必要な値は先にローカル変数へ取り出してから使うこと。

## 数値をいじるとき

- **手触り**: `js/play/rider.js` の `CFG`
- **山**: `js/world/terrain.js` の `ZP`（ゾーン別）、`KICKERS`/`BOXES`（パーク）、`P0`/`A1..A3`（斜度）
- **見た目**: `js/gfx/renderer.js` の `SUN_*`/`AMB_*`/`exposure`、`js/gfx/shaders.js`

## 見た目の確認

数値だけでは分からないので、変更したら実際に描画を見ること。
Playwright を使うなら `executablePath` を環境の Chromium に向ける。
物理を止めて姿勢だけ確認したいときは `window.BLUEBIRD.app.running = false` にして
`window.BLUEBIRD.rider` の値を直接いじると速い。

`window.BLUEBIRD` に rider / camera / chunks / renderer / particles / trails / audio / net / Terrain を出してある。
